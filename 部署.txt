1、选手A、B代码，分别是2个二进制，运行后分别监听两个不同端口a, b
2、我们主办方写另外一个判题器代码，1个二进制，运行后会发送HTTP请求到端口a，b，2个二进制之间交互使用一套协议，用同一套接口文档，判题器会加载地图，开启对局，然后每一回合，会发送当前地图信息（中立方位置、本阵营位置）、本阵营之间英雄、建筑情况（生命值、位置、状态）；等待选手A、B
做出动作响应，每一回合每个英雄只能有一个动作（移动、攻击、采集、建造、外交请求），如此循环往复，每回合判题器给出信息的同时，响应上一次动作的有效性，若动作无效，则给出动作无效原因（比如说移动不能移动出边界、不能攻击自己人、...），直到9000回合为止。1天=300回合，里面240回合是白
天，60回合是晚上。
3、1代码会被上传到一个webui上，后端是一个分布式服务器，会随机把3个二进制放到一台云服务器上的容器中执行